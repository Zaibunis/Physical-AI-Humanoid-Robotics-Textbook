{
  "builds": [
    {
      "src": "book/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "book/build"
      }
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/book-writing/$1" }
  ]
}
