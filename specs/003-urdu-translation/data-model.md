# Data Model: Simple Urdu Translation

This feature is **stateless** and does not require a database.

No data models are defined as per the feature specification, which explicitly scopes out saving translations.
